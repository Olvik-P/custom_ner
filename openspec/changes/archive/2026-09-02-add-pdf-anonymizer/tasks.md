## 1. Зависимости и структура пакета

- [x] 1.1 Добавить опциональную группу зависимостей `pdf` (`PyMuPDF`,
      `pytesseract`) в `pyproject.toml`, бампнуть версию пакета
- [x] 1.2 Создать подпакет `privacyguard_pipeline/pdf/` с пустыми
      `__init__.py`, `text_extractor.py`, `ocr.py`, `renderer.py`,
      `anonymizer.py`

## 2. Извлечение текста с текстового слоя

- [x] 2.1 В `text_extractor.py` реализовать сборку текста построчно/по
      блокам из `page.get_text("dict")`, отдавая связный текст на строку
      вместо сырых font-run span'ов
- [x] 2.2 Построить карту "офсет символа в собранной строке → bbox слова"
      через `page.get_text("words")`
- [x] 2.3 Вернуть единый формат вывода (список слов с bbox и номером
      страницы), общий с OCR-путём (см. design.md, решение 3)
- [x] 2.4 Определить признак "страница без извлекаемого текста" (пустой/почти
      пустой результат) для передачи управления OCR-фолбэку

## 3. OCR-фолбэк для сканов

- [x] 3.1 В `ocr.py` реализовать рендер страницы в изображение и вызов
      `pytesseract.image_to_data` с русской языковой моделью (`lang="rus"`)
- [x] 3.2 Привести вывод Tesseract к тому же формату "слово + bbox +
      страница", что и `text_extractor.py`
- [x] 3.3 Обработать отсутствие системного Tesseract/языкового пакета:
      понятная ошибка с указанием, чего не хватает и как установить (см.
      spec.md, Requirement "OCR-фолбэк для сканированных страниц", сценарий
      "OCR недоступен или не настроен")

## 4. Редакция (настоящее удаление содержимого)

- [x] 4.1 В `renderer.py` реализовать редакцию через
      `page.add_redact_annot(bbox)` + `page.apply_redactions(images=fitz.PDF_REDACT_IMAGE_PIXELS)`
      вместо визуального `draw_rect`
- [x] 4.2 Сгруппировать редакции по странице перед применением (одна
      операция `apply_redactions` на страницу)
- [x] 4.3 Написать/прогнать ручную проверку: скопировать текст из
      обезличенной области выходного PDF и убедиться, что исходное значение
      PII не восстанавливается

## 5. Публичный API и оркестрация

- [x] 5.1 В `anonymizer.py` реализовать `PDFAnonymizer.anonymize()`:
      валидация входного файла → выбор источника слов (текстовый слой или
      OCR) по странице → `PIIDetector.detect()` на собранном тексте → маппинг
      найденных офсетов на bbox слов → редакция → сохранение файла
- [x] 5.2 Реализовать фильтрацию по `entity_types` (см. spec.md, Requirement
      "Выборочная редакция по типу сущности")
- [x] 5.3 Реализовать `PDFAnonymizationResult` (страницы обработаны,
      количество по типам, `success`/`error_message`) без исходных значений
      PII внутри
- [x] 5.4 Валидация входного файла: явная ошибка "файл не найден" до начала
      обработки (см. spec.md, Requirement "Валидация входных данных")
- [x] 5.5 При ошибке в процессе обработки не оставлять на диске частично
      обезличенный файл, который выглядит как завершённый результат (см.
      spec.md, сценарий "Ошибка в процессе обработки")

## 6. Экспорт пакета и изоляция зависимостей

- [x] 6.1 В `pdf/__init__.py` реализовать ленивый импорт `fitz`/`pytesseract`
      с понятной ошибкой ("установите `uv sync --extra pdf`") при отсутствии
- [x] 6.2 Экспортировать `PDFAnonymizer`/`PDFAnonymizationResult` из
      `privacyguard_pipeline/__init__.py`, убедившись, что импорт основного
      пакета без extra `pdf` по-прежнему работает

## 7. Тесты

- [x] 7.1 Создать директорию `tests/` (пока не существует)
- [x] 7.2 Тест: PDF с текстовым слоем и известным PII → PII отсутствует в
      извлечённом тексте выходного файла
- [x] 7.3 Тест: PDF без PII → выходной файл идентичен по содержимому тексту
      (страницы не изменены)
- [x] 7.4 Тест: фильтрация по `entity_types` редактирует только заданные типы
- [x] 7.5 Тест: отсутствующий входной файл → ошибка до обработки
- [x] 7.6 Тест: импорт `privacyguard_pipeline` без extra `pdf` не падает;
      обращение к `PDFAnonymizer` без extra даёт понятную ошибку
- [x] 7.7 (если Tesseract доступен в окружении) тест сканированной страницы
      через OCR-путь — реализован, в этом окружении SKIPPED (нет системного
      Tesseract)

## 8. Документация

- [x] 8.1 Обновить README: установка `uv sync --extra pdf`, требование
      системного Tesseract с `rus.traineddata` для OCR-пути, пример
      использования `PDFAnonymizer`
- [x] 8.2 Явно задокументировать в README, что OCR-путь менее надёжен, чем
      текстовый слой (см. design.md, «Риски / Компромиссы»)

## 9. Устранение дефектов, найденных на реальном документе

- [x] 9.1 Заменить редакцию по bbox отдельного слова на редакцию целого
      PDF-span'а — `apply_redactions()` удаляет текст по границам span'а
      (типично: вся строка при выравнивании по ширине), а не
      прямоугольника, из-за чего соседний не-PII текст на той же строке
      обрезался (см. design.md, Решение 2 и Риски)
- [x] 9.2 Извлекать embedded-шрифт span'а до вызова `apply_redactions()`
      (после редакции PyMuPDF может убрать более не используемый ресурс
      шрифта со страницы)
- [x] 9.3 Сопоставлять имя шрифта из `get_text("dict")` (без
      subset-префикса) с `get_fonts()` (с префиксом вида `BAAAAA+...`)
- [x] 9.4 Добавить fallback на системный шрифт для восстановления не-PII
      текста, когда у извлечённого embedded-шрифта нет рабочей
      Unicode-cmap (типично для subset-шрифтов)
- [x] 9.5 Редактировать span без заливки + рисовать чёрные прямоугольники
      отдельно, только поверх PII-слов (иначе восстановленный текст
      оказывался чёрным по чёрному)
- [x] 9.6 Убрать padding у редакции bbox самого span'а (padding пересекал
      границу плотно расположенной соседней строки и стирал её как
      побочный эффект — тот же класс бага из 9.1, но от собственного
      отступа); padding сохранён только для косметических чёрных
      прямоугольников поверх PII-слов
- [x] 9.7 Исключить URL из редакции по умолчанию (`redact_urls: bool`),
      с приоритетом явно заданного `entity_types` (см. spec.md, Requirement
      "Выборочная редакция по типу сущности")
- [x] 9.8 Тест на восстановление не-PII текста и на дефолтное поведение
      URL (`test_url_kept_by_default_but_redactable_on_request`)
