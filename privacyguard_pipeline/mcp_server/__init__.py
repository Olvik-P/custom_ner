"""Локальный MCP-сервер для PrivacyGuard Pipeline.

Защищает вызывающего MCP-агента (например, Claude Desktop/Code) от
прямого доступа к сырым значениям PII: инструменты этого подпакета
обезличивают текст/файлы до того, как их содержимое попадёт в контекст
агента, а не вокруг вызова внешнего LLM API (для этого уже есть
``http-api``).

Публичная поверхность — сам MCP-сервер (``server``), запускаемый через
``python -m privacyguard_pipeline.mcp_server`` (см. __main__.py). Внутренние
модули (registry, server) не предназначены для прямого импорта извне
этого подпакета.

Требует опциональную группу зависимостей "mcp" (MCP Python SDK).
"""

from __future__ import annotations
